// Placeholder — the real login form, validation, and auth wiring get
// built in Section 2. This just proves the routing pipeline works.
export default function Login() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-navy-950">
      <div className="text-center">
        <h1 className="font-display text-3xl italic text-gold-400">FAJ Prime</h1>
        <p className="text-slate-400 mt-2">Admin — Login page (Section 2)</p>
      </div>
    </div>
  );
}
