// Placeholder — the real dashboard layout (sidebar/topbar) and
// overview content get built in Section 3. This just proves routing.
export default function Dashboard() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-50">
      <div className="text-center">
        <h1 className="font-display text-3xl text-navy-900">Dashboard</h1>
        <p className="text-slate-600 mt-2">Layout + overview page (Section 3)</p>
      </div>
    </div>
  );
}
